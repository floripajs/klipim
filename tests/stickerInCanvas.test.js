var should = require( 'should' );

describe( 'Add sticker in canvas', function() {

    it( 'Testing tests =P', function() {
        var test_true = true;
        return test_true.should.be.ok;
    });

});